/*Tyler Scott
March 22, 2013

Write a program that reads 10 integers and finds the the number of 
positive integers and their average, the number of negative integers and 
their average, and the number of zeros.*/

#include <stdio.h>
int main(){
	
	int posn;
	int negn;
	int average;
	int sum;
	int num;
	int zeros
	
	printf("enter the integers \n", 10);
	scanf("%d", &num);
	if(num > 0){
		sum = posn + num; 
		average = posn/10;
		printf("%d\n", posn);
	}
	else if(num < 0){
		sum = negn + num; 
		average = negn/10;
		printf("%d\n", negn);
	}
	else{
		printf(zeros)
	}
	
}
