#Write a program that randomly generates the 2-D list. Make sizes of the 
#lists users input. Program prints all odd elements.

import random 

def main():
	
	size = int(inpu("enter the size of the lists: "))

	for i in range(row):
		for j in range(col):



	a = [[ 0 for i in range(row)] for j in range(col)] 
 
